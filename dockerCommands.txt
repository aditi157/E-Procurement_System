6. Build Containers

From project root:

docker-compose build
7. Run Containers
docker-compose up

Detached:

docker-compose up -d
8. Stop Containers
docker-compose down
9. Rebuild After Code Changes / Dependency Changes
docker-compose up --build
10. Prisma Migration Inside Container

If needed:

docker exec -it eproc-backend npx prisma migrate deploy

or for dev:

docker exec -it eproc-backend npx prisma migrate dev
11. Access App

Frontend:

http://localhost:5173

Backend API:

http://localhost:5000
12. Common Fixes You May Need
Vite HMR / Host Fix

Already handled via:

npm run dev -- --host
CORS

Ensure backend CORS includes:

origin: "http://localhost:5173"
13. Production-Grade Recommendation (Optional)

Later you can optimize frontend Dockerfile:

Use multi-stage build + nginx.

But for now dev-mode Docker is easiest.

Final Commands Summary
docker-compose build
docker-compose up

Stop:

docker-compose down

Rebuild:

docker-compose up --build